import {searchQuotes} from "../services/QuoteService";

/*export const SEARCH_QUOTES = 'SEARCH_QUOTES';

export const fetchSearchQuotes = text => {
return (dispatch) =>{
    return dispatch({
        type: SEARCH_QUOTES,
        payload: searchQuotes(text)
    })
}
};*/
